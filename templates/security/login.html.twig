{% extends 'layout.html.twig' %}

{% block title %}Into The Wild | Connexion
{% endblock %}
{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('home') }}
{% endblock %}
{% block body %}
    <section class="d-flex justify-content-center mt-5">
        <div class="row text-center">
            <h1 class="title my-5">CONNEXION</h1>
            <h2 class="sub-title my-5">LIBÈRE TON CÔTÉ SAUVAGE AUPRÈS DE</br>PASSIONNÉS DE JEUX VIDÉO</h2>
        </div>
    </section>
<form method="post">
    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    {% if app.user %}
        <div class="mb-3">
            Tu es connecté {{ app.user.email }}, <a href="{{ path('app_logout') }}">Déconnexion</a>
        </div>
    {% endif %}

    <div class="col-lg-3 mx-5 mt-5 text-center">
        <h1 class="title h3 mb-3 font-weight-normal">Connecte toi !</h1>
        <label class="sub-sub-title" for="inputEmail">Email</label>
        <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>
        <label class="sub-sub-title" for="inputPassword">Mot de passe</label>
        <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>

        <input type="hidden" name="_csrf_token"
           value="{{ csrf_token('authenticate') }}">
        <div class="checkbox mb-3">
            <label class="sub-title">
                <input type="checkbox" name="_remember_me"> Se souvenir de moi !
            </label>
        </div>
   

        <button class="connexion btn btn-lg btn-dark my-5" type="submit">
            <h2 class="sub-title">Connexion</h2>
        </button>
        <a href="{{ path('app_register') }}" class="connexion btn btn-lg btn-dark my-5" type="submit">
            <h2 class="sub-title">Pas encore inscrit ?</h2>
        </a>
    </div>
</form>
{% endblock %}
